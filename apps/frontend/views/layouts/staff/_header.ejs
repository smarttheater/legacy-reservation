<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">TIFF <%- __('Online Ticket Reservation') %></a>
        </div>

        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
            </ul>

            <% if (staffUser.isAuthenticated()) {%>
            <a class="btn btn-default navbar-btn navbar-right" href="<%- url('staff.logout', {}) %>">Logout</a>
            <% } %>

            <ul class="nav navbar-nav navbar-right">
            <!--
                <li class="<%- (req.originalUrl === url('Home')) ? 'active' : '' %>"><a href="<%- url('Home') %>">TOP</a></li>
            -->
            </ul>

            <% if (staffUser.isAuthenticated()) {%>
            <p class="navbar-text navbar-right"><a href="<%- url('staff.mypage') %>" class="navbar-link"><%- staffUser.get('signature') %> MyPage</a></p>
            <% } %>

        </div><!--/.nav-collapse -->
    </div>
</nav>
