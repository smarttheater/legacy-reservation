<%
let routeName = '';
if (req.hasOwnProperty('route')) {
    routeName = req.route.name;
}
%>
<header>
    <div class="container">

        <a href="/" class="header-logo"><img src="/images/common/logo.png" alt="TIFF"></a>

        <div class="header-menu">
            <% if (routeName === 'admission.confirm') { %>
            <a class="navbar-brand" href="<%- url('admission.performances') %>"><%- __('Button.Back') %></a>
            <% } %>
        </div>
    </div>
</header>
